[project]
name = "learnings"
version = "0.1.0"
description = "Semantic search over docs/learnings/ files"
requires-python = ">=3.11"
dependencies = [
    "sentence-transformers",
    "numpy",
]

[project.scripts]
learnings = "learnings_pkg.cli:main"
